GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
PAGESPEED_API_KEY=your_pagespeed_api_key_here

# Optional — same key works for both if you enable both APIs in Google Cloud Console
# Enable: Places API (New), Maps JavaScript API, PageSpeed Insights API
# Free tier: $200/month credit = ~6,000 Places requests free
